-- AI Scouting Tips feature dropped (user opted not to pay for an LLM
-- API). This table was empty (0 rows — never successfully generated
-- anything) so there's no data to preserve.
drop table if exists ai_scouting_tips;
